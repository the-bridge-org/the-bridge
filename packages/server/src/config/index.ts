import { __app as app } from "./app";
import { __database as database } from "./database";
import { __graphql as graphql } from "./graphql";
import { __jwt as jwt } from "./jwt";

export const config = { app, database, graphql, jwt };
